version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Installing dependencies if any..."
    build:
      commands:
        - echo "Building the website..."
        - echo "No build process needed - static files only"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*